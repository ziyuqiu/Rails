class Search < ApplicationRecord
	def search_courses
		# courses = Courses.all
		# courses = courses.where(["subject_num LIKE ?", Subjects.where(:num => subject_num)]) if subject.present?
		# return courses

end
